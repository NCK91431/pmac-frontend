# 历史日前申报查询 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成"历史日前申报查询"功能，包括后端 Model/Route 改造、4 个组件 Props+Emit 改造、新 Store/新页面/新组件创建、路由注册

**Architecture:** 提交时存储完整数据快照到 7 个 JSON 字段，历史页面从独立 history store 读取数据并注入到经过 Props+Emit 改造的组件中

**Tech Stack:** Node.js + Express 5 + MySQL (mysql2/promise) + Vue 3 + Pinia + Element Plus

**Spec 参考:** `docs/superpowers/specs/2026-05-25-history-declaration-query-design.md`

---

### Task 1: 后端 Model — 适配 7 个新 JSON 字段

**Files:**
- Modify: `backend-code/models/DailyDeclarationV2.js` (全文件替换)

- [ ] **Step 1: 重写 Model 的 create() 方法**

将 `create()` 的参数从旧的 8 个字段改为新的 7 个字段：

```javascript
const db = require('../config/db');

class DailyDeclarationV2 {
  static async create(data) {
    const {
      user_id,
      declaration_date,
      dateInfo,
      aiQueryText,
      queryResults,
      allSelectedDates,
      priceComparisonData,
      strategyPeriods,
      adjustedRatios,
    } = data;

    const sql = `
      INSERT INTO daily_declaration_v2 (
        user_id, declaration_date,
        dateInfo, aiQueryText, queryResults,
        allSelectedDates, priceComparisonData,
        strategyPeriods, adjustedRatios
      ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
    `;

    const [result] = await db.query(sql, [
      user_id,
      declaration_date,
      dateInfo ? JSON.stringify(dateInfo) : null,
      aiQueryText || null,
      queryResults ? JSON.stringify(queryResults) : null,
      allSelectedDates ? JSON.stringify(allSelectedDates) : null,
      priceComparisonData ? JSON.stringify(priceComparisonData) : null,
      strategyPeriods ? JSON.stringify(strategyPeriods) : null,
      adjustedRatios ? JSON.stringify(adjustedRatios) : null,
    ]);

    return result.insertId;
  }
```

- [ ] **Step 2: 重写 Model 的 update() 方法**

```javascript
  static async update(id, data) {
    const {
      dateInfo,
      aiQueryText,
      queryResults,
      allSelectedDates,
      priceComparisonData,
      strategyPeriods,
      adjustedRatios,
    } = data;

    const sql = `
      UPDATE daily_declaration_v2 SET
        dateInfo = ?,
        aiQueryText = ?,
        queryResults = ?,
        allSelectedDates = ?,
        priceComparisonData = ?,
        strategyPeriods = ?,
        adjustedRatios = ?,
        updated_at = NOW()
      WHERE id = ?
    `;

    const [result] = await db.query(sql, [
      dateInfo ? JSON.stringify(dateInfo) : null,
      aiQueryText || null,
      queryResults ? JSON.stringify(queryResults) : null,
      allSelectedDates ? JSON.stringify(allSelectedDates) : null,
      priceComparisonData ? JSON.stringify(priceComparisonData) : null,
      strategyPeriods ? JSON.stringify(strategyPeriods) : null,
      adjustedRatios ? JSON.stringify(adjustedRatios) : null,
      id,
    ]);

    return result.affectedRows > 0;
  }
```

- [ ] **Step 3: 重写 parseRow() 解析函数**

```javascript
function parseRow(row) {
  try {
    return {
      ...row,
      dateInfo: row.dateInfo ? JSON.parse(row.dateInfo) : null,
      queryResults: row.queryResults ? JSON.parse(row.queryResults) : null,
      allSelectedDates: row.allSelectedDates ? JSON.parse(row.allSelectedDates) : null,
      priceComparisonData: row.priceComparisonData ? JSON.parse(row.priceComparisonData) : null,
      strategyPeriods: row.strategyPeriods ? JSON.parse(row.strategyPeriods) : null,
      adjustedRatios: row.adjustedRatios ? JSON.parse(row.adjustedRatios) : null,
    };
  } catch (jsonError) {
    console.error('daily_declaration_v2 JSON parse error:', jsonError.message);
    return row;
  }
}
```

- [ ] **Step 4: saveOrUpdate() 无需改动**（已通过 data 对象透传）

验证：检查 `saveOrUpdate()` 方法（第 110-136 行），它只使用了 `data.user_id` 和 `data.declaration_date` 做查找，然后透传整个 data 对象给 `create()`/`update()`，无需改动。

---

### Task 2: 后端 Route — 改造 POST /submit

**Files:**
- Modify: `backend-code/routes/dailyDeclarationV2.js` (第 186-225 行)

- [ ] **Step 1: 修改 POST /submit handler**

将旧的字段解构替换为新的 8 个字段（declaration_date + 7 个数据字段）：

```javascript
router.post('/submit', async (req, res) => {
  try {
    const {
      declaration_date,
      dateInfo,
      aiQueryText,
      queryResults,
      allSelectedDates,
      priceComparisonData,
      strategyPeriods,
      adjustedRatios,
    } = req.body;

    if (!declaration_date) {
      return res.status(400).json({ success: false, error: '缺少必填参数 declaration_date' });
    }

    const result = await DailyDeclarationV2.saveOrUpdate({
      user_id: req.user.id,
      declaration_date,
      dateInfo: dateInfo || null,
      aiQueryText: aiQueryText || null,
      queryResults: queryResults || null,
      allSelectedDates: allSelectedDates || null,
      priceComparisonData: priceComparisonData || null,
      strategyPeriods: strategyPeriods || null,
      adjustedRatios: adjustedRatios || null,
    });

    res.json({
      success: true,
      message: result.isUpdate ? '申报方案已更新' : '申报方案提交成功',
      data: {
        id: result.id,
        declaration_date,
        created_at: result.created_at,
      },
    });
  } catch (error) {
    handleError(res, error, '提交申报方案失败');
  }
});
```

- [ ] **Step 2: 验证 GET /history 无需改动**

确认：`GET /history`（第 236-256 行）已通过 `parseRow()` 自动解析 JSON 字段，返回的 `record` 已包含所有新字段（`dateInfo`, `aiQueryText`, `queryResults` 等），前端可直接使用。

---

### Task 3: 前端 Store — 改造 submitDeclaration() 自动组装 payload

**Files:**
- Modify: `frontend-code/src/store/dailyDeclarationV2.js` (第 132-135 行)

- [ ] **Step 1: 将 submitDeclaration 改为无参 + 自动组装 payload**

```javascript
  async function submitDeclaration() {
    const payload = {
      declaration_date: declarationDate.value,
      dateInfo: dateInfo.value,
      aiQueryText: aiQueryText.value,
      queryResults: queryResults.value,
      allSelectedDates: allSelectedDates.value,
      priceComparisonData: priceComparisonData.value,
      strategyPeriods: strategyPeriods.value,
      adjustedRatios: adjustedRatios.value,
    }
    const res = await submitApi(payload)
    return res.data
  }
```

- [ ] **Step 2: 同步修改 index.vue 中 handleConfirmSubmit 的调用方式**

将 `index.vue` 第 407-414 行：
```javascript
async function handleConfirmSubmit() {
  showConfirm.value = false;
  const payload = {
    declaration_date: store.declarationDate,
    periods: (store.strategyPeriods || []).map((p) => ({
      period: p.period,
      adjusted_ratio: store.getAdjustedRatio(p.period),
    })),
  };
  const result = await store.submitDeclaration(payload);
```
改为：
```javascript
async function handleConfirmSubmit() {
  showConfirm.value = false;
  const result = await store.submitDeclaration();
```

---

### Task 4: 前端 Store — 新建 dailyDeclarationHistory.js

**Files:**
- Create: `frontend-code/src/store/dailyDeclarationHistory.js`

- [ ] **Step 1: 创建 history store**

```javascript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { historyDatesApi, historyDetailApi } from '@/DailyDemandReportV2/api'

export const useDailyDeclarationHistoryStore = defineStore('dailyDeclarationHistory', () => {
  const historyDates = ref([])
  const currentDate = ref('')
  const record = ref(null)

  const dateInfo = computed(() => record.value?.dateInfo ?? null)
  const aiQueryText = computed(() => record.value?.aiQueryText ?? '')
  const queryResults = computed(() => record.value?.queryResults ?? [])
  const allSelectedDates = computed(() => record.value?.allSelectedDates ?? [])
  const priceComparisonData = computed(() => record.value?.priceComparisonData ?? null)
  const strategyPeriods = computed(() => record.value?.strategyPeriods ?? [])
  const adjustedRatios = computed(() => record.value?.adjustedRatios ?? [])

  async function fetchHistoryDates() {
    const res = await historyDatesApi()
    if (res.data.success) {
      historyDates.value = res.data.data
    }
  }

  async function fetchRecord(date) {
    const res = await historyDetailApi(date)
    if (res.data.success) {
      record.value = res.data.data
      currentDate.value = date
    }
    return res.data
  }

  return {
    historyDates, currentDate, record,
    dateInfo, aiQueryText, queryResults,
    allSelectedDates, priceComparisonData,
    strategyPeriods, adjustedRatios,
    fetchHistoryDates, fetchRecord,
  }
})
```

---

### Task 5: 组件改造 — QueryResultTable.vue (Props + Emit)

**Files:**
- Modify: `frontend-code/src/DailyDemandReportV2/components/QueryResultTable.vue`

- [ ] **Step 1: 删除 store 引入，添加 props 和 emit 定义**

删除：
```javascript
import { useDailyDeclarationV2Store } from "@/store/dailyDeclarationV2";
const store = useDailyDeclarationV2Store();
```

添加：
```javascript
const props = defineProps({
  results: { type: Array, default: () => [] },
  selectedDateSet: { type: Set, default: () => new Set() },
  readonly: { type: Boolean, default: false },
})

const emit = defineEmits(['select', 'deselect'])
```

- [ ] **Step 2: 替换模板中的 store 引用**

将所有 `store.queryResults` 替换为 `props.results`。

将所有 `store.allSelectedDates` 或 `store.selectedDates` 的查找逻辑替换为 `props.selectedDateSet.has(item.date)`。

将所有复选框 `@change` 从 store 方法调用改为 emit：

```html
<el-checkbox
  :model-value="props.selectedDateSet.has(item.date)"
  :disabled="props.readonly"
  @change="(val) => emit(val ? 'select' : 'deselect', item)"
/>
```

- [ ] **Step 3: 验证 props 默认值**

确保 `results` 默认为空数组 `[]`、`selectedDateSet` 默认为空 Set、`readonly` 默认为 `false`。

---

### Task 6: 组件改造 — SelectedDatesTable.vue (Props + Emit)

**Files:**
- Modify: `frontend-code/src/DailyDemandReportV2/components/SelectedDatesTable.vue`

- [ ] **Step 1: 删除 store 引入，添加 props 和 emit**

删除：
```javascript
import { useDailyDeclarationV2Store } from "@/store/dailyDeclarationV2";
const store = useDailyDeclarationV2Store();
```

添加：
```javascript
const props = defineProps({
  dates: { type: Array, default: () => [] },
  readonly: { type: Boolean, default: false },
})

const emit = defineEmits(['remove'])
```

- [ ] **Step 2: 替换模板中的 store 引用**

将所有 `store.allSelectedDates` 替换为 `props.dates`。

将删除按钮从 `store.removeSelectedDate(item.date)` 改为 `emit('remove', item.date)`。

---

### Task 7: 组件改造 — PriceComparisonTable.vue (Props 驱动)

**Files:**
- Modify: `frontend-code/src/DailyDemandReportV2/components/PriceComparisonTable.vue`

- [ ] **Step 1: 删除 store 引入，添加 props**

删除：
```javascript
import { useDailyDeclarationV2Store } from "@/store/dailyDeclarationV2";
const store = useDailyDeclarationV2Store();
```

添加：
```javascript
const props = defineProps({
  data: { type: Object, default: null },
})
```

- [ ] **Step 2: 替换模板中的 store 引用**

将所有 `store.priceComparisonData` 替换为 `props.data`。注意检查嵌套属性访问如 `store.priceComparisonData?.dates`、`store.priceComparisonData?.periods` 等。

---

### Task 8: 组件改造 — StrategyTableV2.vue (Props + Emit，最复杂)

**Files:**
- Modify: `frontend-code/src/DailyDemandReportV2/components/StrategyTableV2.vue`

- [ ] **Step 1: 添加 props 和 emit 定义**

删除：
```javascript
import { useDailyDeclarationV2Store } from "@/store/dailyDeclarationV2";
const store = useDailyDeclarationV2Store();
```

添加：
```javascript
const props = defineProps({
  periods: { type: Array, default: () => [] },
  summary: { type: Object, default: null },
  declarationDate: { type: String, default: '' },
  adjustedRatios: { type: Array, default: () => [] },
  readonly: { type: Boolean, default: false },
  submitted: { type: Boolean, default: false },
})

const emit = defineEmits(['confirm-edit', 'reset-all'])
```

- [ ] **Step 2: 替换模板中所有 store 引用**

| store 引用 | 替换为 |
|---|---|
| `store.strategyPeriods` | `props.periods` |
| `store.strategySummary` | `props.summary` |
| `store.declarationDate` | `props.declarationDate` |
| `store.adjustedRatios` | `props.adjustedRatios` |
| `store.getAdjustedRatio(p.period)` | `props.adjustedRatios[parseInt(p.period) - 1]` |
| `store.setAdjustedRatio(period, ratio)` | `emit('confirm-edit', { period, ratio })` |
| `store.resetAdjustedRatios()` | `emit('reset-all')` |
| `store.actualQuantities` | 改为纯前端本地计算 |

- [ ] **Step 3: 确认 summary-method 中的读取逻辑**

求和行使用的 `store.strategyPeriods` 改为 `props.periods`，`store.adjustedRatios` 改为 `props.adjustedRatios`。

---

### Task 9: 前端 — 修改 index.vue 组件绑定方式

**Files:**
- Modify: `frontend-code/src/DailyDemandReportV2/index.vue`

- [ ] **Step 1: 更新 QueryResultTable 使用方式**

```html
<QueryResultTable
  :results="store.queryResults"
  :selected-date-set="selectedDateSet"
  :readonly="isStep2Readonly"
  @select="store.selectQueryResult"
  @deselect="(item) => store.deselectQueryResult(item.date)"
/>
```

添加 computed：
```javascript
const selectedDateSet = computed(() => new Set(store.allSelectedDates.map(d => d.date)))
```

- [ ] **Step 2: 更新 SelectedDatesTable 使用方式**

```html
<SelectedDatesTable
  :dates="store.allSelectedDates"
  :readonly="isStep2Readonly"
  @remove="store.removeSelectedDate"
/>
```

- [ ] **Step 3: 更新 PriceComparisonTable 使用方式**

```html
<PriceComparisonTable :data="store.priceComparisonData" />
```

- [ ] **Step 4: 更新 StrategyTableV2 使用方式**

```html
<StrategyTableV2
  ref="strategyTableRef"
  :periods="store.strategyPeriods"
  :summary="store.strategySummary"
  :declaration-date="store.declarationDate"
  :adjusted-ratios="store.adjustedRatios"
  :readonly="isStep3Readonly"
  :submitted="isStep3Submitted"
  @confirm-edit="({ period, ratio }) => store.setAdjustedRatio(period, ratio)"
  @reset-all="store.resetAdjustedRatios"
/>
```

- [ ] **Step 5: 更新 handleConfirmSubmit**

将原有手动组装 payload 的调用改为无参调用：
```javascript
async function handleConfirmSubmit() {
  showConfirm.value = false;
  const result = await store.submitDeclaration();
  if (result.success) {
    store.markStepCompleted(3);
    store.setSubmitted(true);
    showSuccess.value = true;
  } else {
    ElMessage.error('提交失败');
  }
}
```

- [ ] **Step 6: 更新 handleViewHistory 跳转路由**

```javascript
function handleViewHistory() {
  window.open('/daily-demand-report-v2/history', '_blank')
}
```

---

### Task 10: 前端 — 新建 AiQueryTextViewer 组件

**Files:**
- Create: `frontend-code/src/DailyDemandReportV2/components/AiQueryTextViewer.vue`

- [ ] **Step 1: 创建组件**

```vue
<template>
  <div v-if="text" class="ai-query-viewer">
    <div class="aqv-header">
      <span class="aqv-icon">🤖</span>
      <span class="aqv-label">AI 查询语句</span>
    </div>
    <div class="aqv-content">
      <el-tag type="success" effect="plain" class="aqv-tag">查询条件</el-tag>
      <span class="aqv-text">{{ text }}</span>
    </div>
  </div>
</template>

<script setup>
defineProps({
  text: { type: String, default: '' },
})
</script>

<style scoped>
.ai-query-viewer {
  background: linear-gradient(135deg, #f0fff0 0%, #f6ffed 100%);
  border: 1px solid #b7eb8f;
  border-radius: 8px;
  padding: 12px 16px;
  margin-bottom: 16px;
}

.aqv-header {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-bottom: 8px;
}

.aqv-icon {
  font-size: 16px;
}

.aqv-label {
  font-weight: 600;
  color: #389e0d;
  font-size: 13px;
}

.aqv-content {
  display: flex;
  align-items: flex-start;
  gap: 10px;
}

.aqv-tag {
  flex-shrink: 0;
}

.aqv-text {
  color: #303133;
  font-size: 13px;
  line-height: 1.6;
  padding-top: 2px;
}
</style>
```

---

### Task 11: 前端 — 新建 API 函数（已存在，验证即可）

**Files:**
- Verify: `frontend-code/src/DailyDemandReportV2/api/index.js`

- [ ] **Step 1: 确认 API 函数已存在**

验证 `historyDatesApi`（第 28-30 行）和 `historyDetailApi`（第 32-34 行）已正确定义。两个函数已存在，无需新建。

---

### Task 12: 前端 — 新建历史查询页面 + 右侧日历面板

**Files:**
- Create: `frontend-code/src/DailyDemandReportV2History/index.vue`

- [ ] **Step 1: 创建完整页面**

```vue
<template>
  <div class="history-page">
    <div class="history-main" :class="{ 'sidebar-collapsed': !calendarVisible }">
      <div class="history-header">
        <h1>📋 历史日前申报查询</h1>
        <el-button @click="handleBack" class="btn-back">← 返回申报</el-button>
      </div>

      <template v-if="store.record">
        <DateTargetBanner :date-info="store.dateInfo" />
        <AiQueryTextViewer :text="store.aiQueryText" />

        <div class="v2-card v2-card-blue">
          <div class="v2-card-header">
            <div class="card-title">
              查询结果
              <el-tag type="info" size="small">共 {{ store.queryResults.length }} 条记录</el-tag>
            </div>
          </div>
          <div class="v2-card-body">
            <QueryResultTable
              :results="store.queryResults"
              :selected-date-set="selectedDateSet"
              readonly
            />
          </div>
        </div>

        <div class="v2-card v2-card-green">
          <div class="v2-card-header">
            <div class="card-title">
              已选中日期
              <el-tag type="success" size="small">共 {{ store.allSelectedDates.length }} 条记录</el-tag>
            </div>
          </div>
          <div class="v2-card-body">
            <SelectedDatesTable :dates="store.allSelectedDates" readonly />
          </div>
        </div>

        <div class="v2-card" style="border-color: #b7eb8f">
          <div class="v2-card-header" style="padding: 16px 20px 0; border-bottom: none">
            <div class="card-title">
              ⚡ 价差表
              <span class="v2-badge" style="background: #722ed1; color: #fff; border: none">
                选中 {{ store.priceComparisonData?.dates?.length || 0 }} 天
              </span>
            </div>
          </div>
          <PriceComparisonTable :data="store.priceComparisonData" />
        </div>

        <div class="v2-card" style="border-color: #1d39c4">
          <div class="v2-card-header" style="padding: 16px 20px 0; border-bottom: none">
            <div class="card-title">
              📋 日前用电侧申报策略表
              <span class="v2-badge" style="background: #1d39c4; color: #fff; border: none">16列 · 双表头</span>
            </div>
          </div>
          <StrategyTableV2
            :periods="store.strategyPeriods"
            :summary="store.strategySummary"
            :declaration-date="store.currentDate"
            :adjusted-ratios="store.adjustedRatios"
            readonly
            :submitted="true"
          />
        </div>
      </template>

      <div v-else-if="loading" class="v2-loading">
        <el-icon class="is-loading" :size="32"><Loading /></el-icon>
        <p>加载中...</p>
      </div>

      <div v-else class="v2-empty">
        <el-empty description="请从右侧日期面板选择一个已申报的日期" :image-size="120" />
      </div>
    </div>

    <!-- 右侧日历面板 -->
    <div class="calendar-sidebar" :class="{ collapsed: !calendarVisible }">
      <div class="calendar-toggle" @click="calendarVisible = !calendarVisible">
        <span v-if="calendarVisible">➡ 收起</span>
        <span v-else>📅 日历</span>
      </div>

      <div v-show="calendarVisible" class="calendar-content">
        <div class="calendar-title">已申报日期</div>

        <div v-for="year in yearList" :key="year" class="cal-year">
          <div class="cal-year-title" @click="toggleYear(year)">
            {{ year }} 年
            <span class="cal-arrow" :class="{ expanded: expandedYear === year }">▶</span>
          </div>

          <div v-show="expandedYear === year" class="cal-months">
            <div v-for="month in 12" :key="month" class="cal-month">
              <div
                class="cal-month-title"
                @click="toggleMonth(year, month)"
              >
                {{ month }} 月
                <span class="cal-arrow" :class="{ expanded: expandedMonthKey === `${year}-${month}` }">▶</span>
              </div>

              <div v-show="expandedMonthKey === `${year}-${month}`" class="cal-days">
                <div
                  v-for="day in daysInMonth(year, month)"
                  :key="day"
                  class="cal-day"
                  :class="{
                    'has-record': hasRecord(year, month, day),
                    'is-current': store.currentDate === formatDate(year, month, day),
                  }"
                  @click="selectDate(year, month, day)"
                >
                  <span class="day-num">{{ day }}</span>
                  <span v-if="hasRecord(year, month, day)" class="day-check">✅</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { Loading } from '@element-plus/icons-vue'
import { useDailyDeclarationHistoryStore } from '@/store/dailyDeclarationHistory'

import DateTargetBanner from '@/DailyDemandReportV2/components/DateTargetBanner.vue'
import AiQueryTextViewer from '@/DailyDemandReportV2/components/AiQueryTextViewer.vue'
import QueryResultTable from '@/DailyDemandReportV2/components/QueryResultTable.vue'
import SelectedDatesTable from '@/DailyDemandReportV2/components/SelectedDatesTable.vue'
import PriceComparisonTable from '@/DailyDemandReportV2/components/PriceComparisonTable.vue'
import StrategyTableV2 from '@/DailyDemandReportV2/components/StrategyTableV2.vue'

const router = useRouter()
const store = useDailyDeclarationHistoryStore()

const calendarVisible = ref(true)
const expandedYear = ref(new Date().getFullYear())
const expandedMonthKey = ref(`${new Date().getFullYear()}-${new Date().getMonth() + 1}`)
const loading = ref(false)

const currentYear = new Date().getFullYear()
const yearList = Array.from({ length: currentYear - 2026 + 1 }, (_, i) => 2026 + i).reverse()

const historyDateSet = computed(() => new Set(store.historyDates))

const selectedDateSet = computed(() => new Set(store.allSelectedDates.map(d => d.date)))

function toggleYear(year) {
  expandedYear.value = expandedYear.value === year ? null : year
  expandedMonthKey.value = null
}

function toggleMonth(year, month) {
  const key = `${year}-${month}`
  expandedMonthKey.value = expandedMonthKey.value === key ? null : key
  if (expandedYear.value !== year) {
    expandedYear.value = year
  }
}

function daysInMonth(year, month) {
  return new Date(year, month, 0).getDate()
}

function formatDate(year, month, day) {
  return `${year}-${String(month).padStart(2, '0')}-${String(day).padStart(2, '0')}`
}

function hasRecord(year, month, day) {
  return historyDateSet.value.has(formatDate(year, month, day))
}

async function selectDate(year, month, day) {
  const dateStr = formatDate(year, month, day)
  if (!hasRecord(year, month, day)) return

  loading.value = true
  await store.fetchRecord(dateStr)
  loading.value = false
}

function handleBack() {
  router.push('/daily-demand-report-v2')
}

onMounted(async () => {
  await store.fetchHistoryDates()
})
</script>

<style scoped lang="scss">
.history-page {
  display: flex;
  min-height: calc(100vh - 60px);
  position: relative;
}

.history-main {
  flex: 1;
  padding: 24px;
  transition: margin-right 0.3s;
  max-width: calc(100% - 320px);

  &.sidebar-collapsed {
    max-width: 100%;
  }
}

.history-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;

  h1 {
    font-size: 20px;
    color: #1d39c4;
    margin: 0;
  }
}

.btn-back {
  background: #fff;
  border-color: #d9d9d9;
  color: #666;
}

// 日历侧边栏
.calendar-sidebar {
  position: fixed;
  right: 0;
  top: 60px;
  width: 320px;
  height: calc(100vh - 60px);
  background: #fff;
  border-left: 1px solid #e8e8e8;
  box-shadow: -2px 0 8px rgba(0, 0, 0, 0.06);
  z-index: 1000;
  transition: width 0.3s;
  overflow-y: auto;

  &.collapsed {
    width: 48px;
  }
}

.calendar-toggle {
  padding: 12px;
  cursor: pointer;
  font-size: 13px;
  color: #1d39c4;
  font-weight: 500;
  text-align: center;
  border-bottom: 1px solid #f0f0f0;
  user-select: none;

  &:hover {
    background: #f0f5ff;
  }
}

.calendar-content {
  padding: 12px;
}

.calendar-title {
  font-size: 14px;
  font-weight: 600;
  color: #303133;
  margin-bottom: 12px;
  padding-left: 4px;
}

.cal-year {
  margin-bottom: 4px;
}

.cal-year-title {
  padding: 6px 8px;
  cursor: pointer;
  font-weight: 600;
  font-size: 14px;
  color: #303133;
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: space-between;

  &:hover {
    background: #f5f5f5;
  }
}

.cal-arrow {
  font-size: 10px;
  transition: transform 0.2s;
  color: #8c8c8c;

  &.expanded {
    transform: rotate(90deg);
  }
}

.cal-months {
  padding-left: 12px;
}

.cal-month {
  margin-bottom: 2px;
}

.cal-month-title {
  padding: 5px 8px;
  cursor: pointer;
  font-size: 13px;
  color: #555;
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: space-between;

  &:hover {
    background: #f5f5f5;
  }
}

.cal-days {
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
  padding: 6px 4px 6px 16px;
}

.cal-day {
  width: 36px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  border-radius: 4px;
  font-size: 12px;
  color: #8c8c8c;
  position: relative;

  .day-num {
    line-height: 1;
  }

  .day-check {
    font-size: 8px;
    position: absolute;
    top: -2px;
    right: -2px;
  }

  &.has-record {
    color: #303133;
    font-weight: 500;
    cursor: pointer;

    &:hover {
      background: #e6f7ff;
    }
  }

  &.is-current {
    background: #1890ff;
    color: #fff;

    &:hover {
      background: #40a9ff;
    }
  }
}

.v2-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80px 0;
  color: #8c8c8c;
}

.v2-empty {
  padding: 80px 0;
}

// 复用申报页面的卡片样式
.v2-card {
  border: 1px solid #e8e8e8;
  border-radius: 8px;
  margin-bottom: 16px;
  background: #fff;

  &.v2-card-blue {
    border-color: #91d5ff;
  }

  &.v2-card-green {
    border-color: #b7eb8f;
  }

  .v2-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 20px;
    border-bottom: 1px solid #f0f0f0;

    .card-title {
      display: flex;
      align-items: center;
      gap: 8px;
      font-weight: 600;
      font-size: 14px;
      color: #303133;
    }
  }

  .v2-card-body {
    padding: 16px 20px;
  }
}
</style>
```

---

### Task 13: 前端 — 注册路由和侧边栏

**Files:**
- Modify: `frontend-code/src/router/index.js`（路由 + 侧边栏）

- [ ] **Step 1: 添加历史查询路由**

在 `/daily-demand-report-v2` 路由后面添加：
```javascript
{
  path: "/daily-demand-report-v2/history",
  name: "DailyDemandReportV2History",
  component: () => import("@/DailyDemandReportV2History/index.vue"),
  meta: {
    module: "trading",
    permission: "internal",
    breadcrumb: {
      title: "历史日前申报查询",
      icon: "bi bi-card-heading",
      parentRouteName: "DailyDemandReportV2",
    },
  },
},
```

- [ ] **Step 2: 在 trading 侧边栏添加历史入口**

在 `sidebarMenuConfig.trading.children` 中添加：
```javascript
{ title: "历史日前申报查询", route: "/daily-demand-report-v2/history" },
```

---

## Self-Review

### 1. Spec 覆盖率检查

| Spec 要求 | 对应 Task | 状态 |
|---|---|---|
| 数据库改为 7 个新 JSON 字段 | 用户已自行执行 SQL | ✅ 外部完成 |
| 后端 Model 适配新字段 | Task 1 | ✅ |
| 后端 Route submit 接收新 payload | Task 2 | ✅ |
| GET /history 无需改动（parseRow 已兼容） | Task 2 Step 2 | ✅ |
| Store submitDeclaration() 无参自动组装 | Task 3 | ✅ |
| 新建 dailyDeclarationHistory store | Task 4 | ✅ |
| QueryResultTable props+emit | Task 5 | ✅ |
| SelectedDatesTable props+emit | Task 6 | ✅ |
| PriceComparisonTable props | Task 7 | ✅ |
| StrategyTableV2 props+emit | Task 8 | ✅ |
| index.vue 组件绑定更新 | Task 9 | ✅ |
| index.vue handleConfirmSubmit 无参调用 | Task 9 Step 5 | ✅ |
| index.vue handleViewHistory 改为跳转 | Task 9 Step 6 | ✅ |
| AiQueryTextViewer 新建组件 | Task 10 | ✅ |
| API 函数已存在（验证） | Task 11 | ✅ |
| 历史页面 + 6 个组件展示 | Task 12 | ✅ |
| 右侧日历面板（年-月-日手风琴） | Task 12 | ✅ |
| 路由注册 | Task 13 | ✅ |
| 侧边栏菜单 | Task 13 | ✅ |
| 2026 年至当前年份 | Task 12 `yearList` | ✅ |
| 默认展开当前年当前月 | Task 12 `expandedYear`/`expandedMonthKey` | ✅ |
| 已申报日期 ✅ 标记 | Task 12 `hasRecord` | ✅ |
| 日历年/月/日手风琴模式 | Task 12 `toggleYear`/`toggleMonth` | ✅ |

### 2. Placeholder 扫描

确认所有步骤包含完整代码，无"TBD"、"TODO"、"implement later"等占位符。

### 3. 类型一致性检查

- `adjustedRatios` 在 Store、Model、Route、组件 props 中统一为 `Array`
- `priceComparisonData` 在 Store、Model、Route、组件 props 中统一为 `Object|null`
- `dateInfo` 在 Store、Model、Route 中统一为 `Object|null`
- 所有组件 emit 事件名统一为 kebab-case（`confirm-edit`, `reset-all`）
- emit payload 结构：`confirm-edit` 统一为 `{ period, ratio }` 对象
